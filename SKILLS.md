# 项目技能

本文列出后续 Codex 运行时应遵守的仓库专属技能。这些是项目约定，不是外部依赖包。

## assessment-pdf-generator

用于实现、调试或评审试卷生成、练习单生成、PDF 导出、答案解析 PDF 或可打印 A4 版式。

核心流程：

1. 读取教师需求，并映射为学科、年级、类型、难度、目标学生/班级、教材上下文和特殊要求。
2. 只要求 DeepSeek 返回结构化 JSON。
3. DeepSeek 输出后运行 Codex 修复：
   - 解析或恢复 JSON；
   - 强制符合学科蓝图；
   - 补齐缺失题目，直到达到最低题量；
   - 删除未被要求的附加题；
   - 补齐答案、解析步骤、易错点、考点、选项和图形元数据；
   - 对客观题、写作题、听力题限制或移除作答空间。
4. 持久化标准化后的作业题目。
5. 导出两个文件：学生作答卷和答案解析卷。
6. 检查输出，重点关注页面密度、编号、分区、图形、作答空间和解析质量。

学科蓝图：

- 数学试卷：填空题、选择题、计算题、解答题。默认不加附加题。
- 语文试卷：基础知识、积累与应用、阅读理解、写作题。五六年级可以包含文言文；三四年级默认不加文言文。
- 英语试卷：单项选择题、词汇运用、阅读理解、写作。听力只在教师作为特殊要求提出时加入。
- 英语阅读理解必须有完整的文章、对话或材料：试卷至少 2 篇，五六年级每篇原则上不少于 130 个英文单词，三四年级每篇原则上不少于 90 个英文单词；题目要覆盖细节理解、推断、语境词义、主旨/标题、顺序/因果，不能用单句让学生猜答案。
- 语文阅读理解必须有完整短文或材料；五六年级可加入短小文言文。阅读题要要求学生联系上下文、概括、推断、赏析或表达启示。

生成类型区分：

- 练习：按学生近期错题、薄弱点、教师指定需求或个性化学习目标生成；题型服务于巩固和补弱，可以围绕少数知识点做梯度练习，不套用完整试卷结构。
- 小测：按年级教材单元、课本章节、当周教学内容或教师指定范围生成；内容应短、集中、贴近单元目标。例如英语单元小测可包含中英文互译、写单词、造句、少量选择题和一篇短阅读，而不是默认套用试卷模板。
- 试卷：用于单元测试、月考、期中、期末、小升初或综合阶段测评；需要覆盖多个板块和能力层级，按对应年级与考试目标组织完整卷面结构。
- 生成服务必须先识别 `kind` 和教师要求，再选择模板密度、题型组合和作答空间。小测、练习、试卷必须有明确区分度，不得把同一套试卷模板简单缩放后用于所有类型。
- 英语小测不得默认使用文章选词填空、完形填空、短文语法填空或写作；英语练习也不得默认套用这些试卷题型，除非教师明确要求。英语试卷才默认使用文章选词填空、短文语法填空或完形填空、正式阅读和写作。中译英、根据中文写英文单词或短语、造句和中译英句子补全必须用题干在上、英语四线格在下的上下结构作答，不得用普通横线替代。
- 语文小测/练习不得出现作文题；阅读材料必须有完整上下文，五六年级现代文阅读应有足够长度，文言文只在试卷或教师明确要求时加入。
- 数学小测/练习要减少无效题量堆叠，优先给计算题、解答题和图形题保留可书写空间。

版式规则：

- 小测/练习默认 2 页 A4。
- 试卷默认 4 页 A4。
- 生成的学生卷中，非写作板块题量应尽量为偶数；在教师要求的写作/作文题量允许时，学生卷总题量也应保持偶数。不得只为凑偶数而添加备用作文。
- 每次生成都必须避免小测、练习、试卷之间以及后续生成之间出现同质化或重复题目。服务层必须优先使用新的文章、新的词库、新的数字、新的情境和新的写作提示；外部或通用结构只能作为版式和题型灵感，导出前必须重新创作原创题目。
- 不得用空白区域伪造页数。
- 学生卷必须为真实作答保留足够空间，同时避免大量未使用空白；导出前必须综合平衡页面密度、作答空间大小和分页。
- 选择题应尽量使用紧凑选项。
- 填空题、选择题、判断题、听力题和写作提示题不应生成大块作答框。
- 计算题和解答题需要干净的留白演算区。
- 数学图形题的图形必须统一渲染在题目区域右下角；不得居中漂浮、挤占题干主阅读区，且必须和对应小题绑定在同一题块内。
- 阅读题只在确有需要时保留简答空间。
- 写作题只应显示标题、提示、字数和要求；除非教师明确要求，不要预留完整作文纸。
- `如图` 必须有图形元数据和已渲染图形。

命令安全：

- 避免用一次性长命令查看大范围差异、日志或多关键词搜索；工具输出可能被截断，Windows 的 `cmd /c` 会把未转义的 `|` 当作管道符。
- 优先使用 `git diff --stat`、`git diff --name-status`、限定路径的差异命令和拆开的 `rg` 调用。复杂查询需要写入已纳入管理或临时的 Node 脚本，不要压成密集的一行 shell 命令。

解析卷规则：

- 每道题都需要答案、解析步骤、考点和易错点。
- 英语阅读和写作要包含推理依据或评分要点。
- 听力题的听力材料/原文应放在解析卷中；除非教师要求，不放在学生卷中。
- 如果 DeepSeek 返回题目过少、缺少阅读材料、依赖图形的题目没有图形，或解析只有答案，Codex/服务层必须先修复，再持久化或导出。

验证：

- 修改资料摄取、内容索引、提示词/上下文生成、试卷草稿导出、教师复核或最终学生卷/解析卷导出时，运行 `cmd /c npm.cmd run check:content-context`。
- 修改教师端资料上传面板或网页端上传接口封装时，运行 `cmd /c npm.cmd run check:content-upload-ui`。
- 修改同时涉及教师上传界面和内容生成/导出链路时，运行 `cmd /c npm.cmd run check:teaching-content`。
- 运行 `npm.cmd run check --workspace apps/api`。
- 网页端变更后运行 `npm.cmd run typecheck --workspace apps/web`。
- 运行 `npm.cmd run check:encoding`。
- 生成一份真实试卷，并检查导出的 HTML/PDF 内容。

## ai-service-orchestrator

用于修改 AI 服务商集成、降级行为，或修改网页端、小程序、课堂平板共享的服务契约。

职责：

- 网页端只视为原型和测试前端；共享行为必须沉淀在接口、服务、脚本或明确的数据契约中，供小程序、课堂平板和公共屏复用。
- DeepSeek 负责主要文本生成。
- MiniMax 在可用时负责语音和后续多模态工作。
- Codex/GPT-5.5 负责对模型服务商输出进行编排、校验、纠错和补全。
- 绝不把模型服务商的原始半成品作为最终用户可见内容保存或展示。
- 家长/学生界面不得显示模型或服务商名称。
- 教师操作界面可以展示服务商状态和诊断信息。

降级预期：

- 如果模型服务商输出不可用，应为交互功能创建清晰的不可用状态。
- 如果模型服务商输出可用但不完整，必须先修复，再保存或导出。
- 如果生成的结构化数据格式错误，应恢复可用内容，并用确定性方式补齐必填字段。
- 如果 OCR/批改输出不确定，应标记为需要教师复核并保留证据，不要编造确定性。

持久化预期：

- 在可行范围内，将生成任务、试卷、批改、问答、平板互动和画像草稿绑定到学生。
- 学生/家长端发布画像反馈前必须保留教师复核。
- 审计日志要有用，但不能向学习者泄露模型服务商内部信息。

## prompt-context-engineering

用于修改提示词、模型输入、生成内容质量、批改行为、学生画像草稿、生成上下文，或任何要求 DeepSeek/MiniMax/Codex 生成或修复教育内容的流程。

主要参考：

- `docs/41-prompt-context-engineering-playbook.md`

规则：

- 提示词和上下文契约必须保持平台中立。网页端可以测试流程，但小程序、课堂平板和公共屏客户端必须能够复用同一份 `generationContext`、复核状态和可见性规则。
- 提示词工程指模型服务商任务的文字表达；上下文工程指提供给模型服务商的结构化数据包。
- 当服务可以同时提供年级、学科、学生范围、教材章节、近期错题、任务、答案键、版式目标、可见性规则和教师复核要求时，不要只发送教师的自由文本需求。
- 优先使用包含 `request`、`target`、`teaching`、`studentSignals`、`output` 和 `rules` 的 `generationContext` 结构。
- 模型服务商输出只是草稿。持久化或导出前，始终要执行解析、标准化、校验、修复和复核状态处理。
- 学生/家长可见内容必须使用 `AI生成`，不得暴露服务商、模型名、内部提示词、思维链或未发布的未来功能。
- 可打印测评的流程是：草稿内容 -> 草稿 PDF 导出 -> 教师是/否复核 -> 正式学生 PDF + 解析 PDF 导出。
- 批改场景中，不确定的 OCR 或不清晰的学生答案必须保持 `uncertain` 并要求教师复核；不得编造确定性。
- 学生画像场景中，先生成可供教师复核的草稿，教师确认后才发布。

## encoding-and-windows-guard

用于在 Windows 上编辑中文界面文案、接口响应、生成文档、项目文档或脚本。

规则：

- 避免使用 PowerShell 执行复杂中文命令。
- npm 脚本优先使用 `cmd /c npm.cmd ...`。
- 多行逻辑优先使用已纳入管理的 Node 脚本或 Node REPL。
- 文件编辑使用 `apply_patch`。
- 中文文本变更后运行 `npm.cmd run check:encoding`。
- 生成或调试出现乱码的可打印 HTML/PDF 产物后，运行 `npm.cmd run check:encoding:generated`。
- 修改内容索引、编码守卫、提示词/上下文组装或测评导出/复核流程后，运行 `cmd /c npm.cmd run check:content-context`。
- 重复替换问号、中文标题乱码等乱码问题，或混合编码痕迹，必须视为家长/学生/教师可见输出的阻断缺陷。
